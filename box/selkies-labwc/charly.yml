box:
    name: selkies-labwc
    # CPU streaming desktop on the CachyOS (Arch) base — pixi build of selkies
    # (pixelflux Rust) + AUR google-chrome/wlrctl + labwc/waybar/swaync/etc.
    base: cachyos.cachyos
    builder:
        pixi: charly.arch-builder
        npm: charly.arch-builder
        cargo: charly.arch-builder
        aur: charly.arch-builder
    build:
        - pac
        - aur
    platform:
        - linux/amd64
    candy:
        - '@github.com/overthinkos/overthink/candy/agent-forwarding:v2026.160.2017'
        - '@github.com/overthinkos/overthink/candy/selkies-desktop:v2026.160.2017'
        - '@github.com/overthinkos/overthink/candy/dbus:v2026.160.2017'
    port:
        - 3000:3000
        - 9222:9222
        - 9224:9224
        - 2222:2222
    eval:
        - command: test -x /usr/bin/labwc && test -x /usr/bin/google-chrome-stable && test -x /usr/local/bin/traefik && test -x /usr/sbin/sshd
          id: selkies-labwc-binaries
          exit_status: 0
        - addr: 127.0.0.1:${HOST_PORT:9224}
          id: selkies-labwc-chrome-devtools-mcp-port
          timeout: 5s
          scope: deploy
          reachable: true
